import { notFound } from "next/navigation"; import Link from "next/link"; import { ArrowLeft, Award, CalendarDays, IndianRupee, Phone, Users } from "lucide-react"; import { Avatar, Badge, SectionTitle, StatCard } from "@/components/ui"; import { currency } from "@/lib/utils"; import { getTrainerById } from "@/services";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const t=await getTrainerById(id);if(!t)notFound();return <><div className="profile-back"><Link href="/trainers"><ArrowLeft/> Back to trainers</Link></div><section className="profile-hero"><div className="profile-person"><Avatar src={t.image} name={t.name} size={120}/><div><Badge status={t.status}/><h1>{t.name}</h1><p>{t.specialization} · {t.experience} years experience</p><div className="contact-row"><span><Phone/>{t.phone}</span><span>{t.email}</span></div></div></div><button className="btn primary">Assign members</button></section><div className="stat-grid compact"><StatCard label="Assigned members" value={String(t.assignedMembers)} detail="6 new this month" icon={<Users/>}/><StatCard label="Today’s classes" value="4" detail="2 completed" icon={<CalendarDays/>} tone="blue"/><StatCard label="Member rating" value="4.9 / 5" detail="86 reviews" icon={<Award/>} tone="violet"/><StatCard label="Monthly salary" value={currency(t.salary)} detail="Paid on 1st" icon={<IndianRupee/>} tone="orange"/></div><div className="chart-grid"><section className="panel"><SectionTitle title="Today’s schedule"/><div className="timeline">{[["06:00","Strength Lab","12 members"],["08:00","Personal Training","Rahul Sharma"],["17:00","Functional Fitness","18 members"],["19:00","HIIT","22 members"]].map(x=><div key={x[0]}><time>{x[0]}</time><span><b>{x[1]}</b><small>{x[2]}</small></span></div>)}</div></section><section className="panel"><SectionTitle title="Performance"/><div className="body-bars">{[["Member retention",92],["Attendance",96],["Goal completion",84],["Member rating",98]].map(x=><div key={x[0] as string}><span>{x[0]}</span><b>{x[1]}%</b><div className="progress-track"><i style={{width:`${x[1]}%`}}/></div></div>)}</div></section></div></>}
